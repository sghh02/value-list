name: valuelist
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  go_router: ^12.1.1        # ルーティング
  flutter_riverpod: ^2.3.7  # 状態管理, 依存性注入
  mockito: ^5.4.0           # テスト
  # 多言語関連
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/images/logo.png
    - assets/images/human-img.png
#  fonts:
#    - family: CustomFont
#      fonts:
#        - asset: assets/fonts/custom_font.ttf
#l10n:
#  arb-dir: lib/l10n
#  template-arb-file: app_en.arb
#  output-localization-file: S.dart
#  output-class: S
